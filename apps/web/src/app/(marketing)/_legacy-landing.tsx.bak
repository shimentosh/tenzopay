import Link from 'next/link';
import {
  ArrowRight,
  Ban,
  Gauge,
  Layers,
  Lock,
  ReceiptText,
  ScrollText,
  Snowflake,
} from 'lucide-react';
import { Button } from '@/components/ui/button';
import { VirtualCard } from '@/components/virtual-card';
import type { CardSummary } from '@tenzopay/shared';

/** Illustrative cards for the hero. Clearly sample data, not a live balance. */
const showcase: CardSummary[] = [
  {
    id: '1',
    name: 'Marketing',
    lastFour: '4821',
    expMonth: '09',
    expYear: '2030',
    network: 'VISA',
    status: 'ACTIVE',
    dailyLimit: '50000',
    monthlyLimit: '500000',
    perTransactionLimit: '25000',
    spentToday: '18400',
    spentThisMonth: '214000',
    createdAt: new Date().toISOString(),
  },
  {
    id: '2',
    name: 'SaaS Subscriptions',
    lastFour: '1179',
    expMonth: '04',
    expYear: '2031',
    network: 'VISA',
    status: 'ACTIVE',
    dailyLimit: '10000',
    monthlyLimit: '120000',
    perTransactionLimit: '10000',
    spentToday: '2900',
    spentThisMonth: '86500',
    createdAt: new Date().toISOString(),
  },
  {
    id: '3',
    name: 'Travel',
    lastFour: '6034',
    expMonth: '11',
    expYear: '2029',
    network: 'VISA',
    status: 'FROZEN',
    dailyLimit: '100000',
    monthlyLimit: '800000',
    perTransactionLimit: '100000',
    spentToday: '0',
    spentThisMonth: '312000',
    createdAt: new Date().toISOString(),
  },
];

export default function HomePage() {
  return (
    <>
      {/* ------------------------------------------------------------ Hero */}
      <section className="relative overflow-hidden">
        <div
          aria-hidden
          className="pointer-events-none absolute inset-x-0 top-0 h-[520px] bg-[radial-gradient(60%_100%_at_50%_0%,var(--accent),transparent_70%)]"
        />

        <div className="relative mx-auto grid max-w-6xl items-center gap-14 px-5 py-16 lg:grid-cols-[1.05fr_1fr] lg:py-24">
          <div className="animate-in-up">
            <span className="inline-flex items-center gap-2 rounded-full border border-border bg-card px-3 py-1 text-xs font-medium text-muted-foreground">
              <span className="size-1.5 rounded-full bg-positive" aria-hidden />
              Virtual cards, issued in seconds
            </span>

            <h1 className="mt-5 text-4xl font-semibold leading-[1.08] tracking-tight text-foreground sm:text-5xl lg:text-[3.4rem]">
              One Balance.
              <br />
              Every Card.
            </h1>

            <p className="mt-5 max-w-lg text-[17px] leading-relaxed text-muted-foreground">
              Manage your money and multiple spending cards from one simple
              account. Give each card its own limits, freeze it in a tap, and
              see exactly where every unit went.
            </p>

            <div className="mt-8 flex flex-wrap gap-3">
              <Button asChild size="lg">
                <Link href="/signup">
                  Get started
                  <ArrowRight className="size-4" aria-hidden />
                </Link>
              </Button>
              <Button asChild size="lg" variant="secondary">
                <Link href="/virtual-cards">Explore cards</Link>
              </Button>
            </div>

            <dl className="mt-12 grid max-w-md grid-cols-3 gap-6 border-t border-border pt-7">
              {[
                ['1', 'Balance to track'],
                ['20', 'Cards per account'],
                ['3', 'Layers of spend control'],
              ].map(([value, label]) => (
                <div key={label}>
                  <dt className="tnum text-2xl font-semibold text-foreground">
                    {value}
                  </dt>
                  <dd className="mt-0.5 text-xs leading-snug text-muted-foreground">
                    {label}
                  </dd>
                </div>
              ))}
            </dl>
          </div>

          {/* Fanned cards — the product, made literal. */}
          <div className="relative mx-auto w-full max-w-md lg:max-w-none">
            <div className="relative h-[380px] sm:h-[420px]">
              <div className="absolute left-0 top-0 w-[78%] rotate-[-7deg] opacity-70 transition-transform duration-500 hover:rotate-[-9deg]">
                <VirtualCard card={showcase[2]} compact />
              </div>
              <div className="absolute left-[12%] top-[16%] w-[80%] rotate-[3deg] opacity-90">
                <VirtualCard card={showcase[1]} compact />
              </div>
              <div className="absolute left-[6%] top-[34%] w-[86%] transition-transform duration-500 hover:-translate-y-2">
                <VirtualCard card={showcase[0]} />
              </div>
            </div>
          </div>
        </div>
      </section>

      {/* -------------------------------------------------------- The idea */}
      <section className="border-y border-border bg-card">
        <div className="mx-auto max-w-6xl px-5 py-16 lg:py-20">
          <div className="max-w-2xl">
            <h2 className="text-2xl font-semibold tracking-tight text-foreground sm:text-3xl">
              Cards are spending instruments, not separate wallets
            </h2>
            <p className="mt-3 text-[17px] leading-relaxed text-muted-foreground">
              Every card draws on the same balance. You are never moving money
              between pots or topping cards up — you set a limit, and the card
              spends within it.
            </p>
          </div>

          <div className="mt-12 grid gap-6 md:grid-cols-3">
            {[
              {
                icon: Layers,
                title: 'One balance',
                body: 'Deposit once. Every card you issue spends from the same available balance, so you always know your true position.',
              },
              {
                icon: Gauge,
                title: 'Per-card limits',
                body: 'Set a per-transaction, daily and monthly ceiling on each card. Limits are enforced by the card network, not just by our interface.',
              },
              {
                icon: Snowflake,
                title: 'Freeze instantly',
                body: 'Freezing a card stops authorizations at the network. Unfreeze whenever you are ready — nothing is lost.',
              },
            ].map((item) => (
              <div key={item.title} className="card-surface p-6">
                <div className="flex size-10 items-center justify-center rounded-xl bg-accent text-primary">
                  <item.icon className="size-5" aria-hidden />
                </div>
                <h3 className="mt-4 font-semibold text-foreground">{item.title}</h3>
                <p className="mt-2 text-sm leading-relaxed text-muted-foreground">
                  {item.body}
                </p>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* ------------------------------------------------------ How it works */}
      <section className="mx-auto max-w-6xl px-5 py-16 lg:py-20">
        <h2 className="text-2xl font-semibold tracking-tight text-foreground sm:text-3xl">
          How it works
        </h2>

        <ol className="mt-10 grid gap-8 md:grid-cols-4">
          {[
            ['Create your account', 'Sign up and complete identity verification.'],
            ['Add USDT', 'Deposit to your address. We wait for network confirmations before crediting.'],
            ['Issue cards', 'Name each card and set its limits. It is ready immediately.'],
            ['Spend and track', 'Watch every authorization, settlement and refund in one feed.'],
          ].map(([title, body], index) => (
            <li key={title} className="relative">
              <span className="tnum flex size-8 items-center justify-center rounded-full bg-ink-900 text-sm font-semibold text-white">
                {index + 1}
              </span>
              <h3 className="mt-4 font-semibold text-foreground">{title}</h3>
              <p className="mt-1.5 text-sm leading-relaxed text-muted-foreground">{body}</p>
            </li>
          ))}
        </ol>
      </section>

      {/* ---------------------------------------------------------- Control */}
      <section className="border-t border-border bg-card">
        <div className="mx-auto max-w-6xl px-5 py-16 lg:py-20">
          <div className="grid gap-12 lg:grid-cols-2 lg:items-center">
            <div>
              <h2 className="text-2xl font-semibold tracking-tight text-foreground sm:text-3xl">
                Control that actually holds
              </h2>
              <p className="mt-3 text-[17px] leading-relaxed text-muted-foreground">
                A limit is only meaningful if something enforces it. TenzoPay
                checks every authorization against your real balance before it
                is approved.
              </p>

              <ul className="mt-8 space-y-5">
                {[
                  {
                    icon: Ban,
                    title: 'Declined when the balance is short',
                    body: 'Cards share one pool, so five cards cannot spend five times your balance.',
                  },
                  {
                    icon: ReceiptText,
                    title: 'Every movement recorded',
                    body: 'Authorizations, settlements, reversals and refunds each leave a permanent entry.',
                  },
                  {
                    icon: ScrollText,
                    title: 'Nothing changes silently',
                    body: 'Balances are derived from recorded history, never edited in place.',
                  },
                ].map((item) => (
                  <li key={item.title} className="flex gap-4">
                    <div className="mt-0.5 flex size-9 shrink-0 items-center justify-center rounded-lg bg-muted text-foreground/80">
                      <item.icon className="size-[18px]" aria-hidden />
                    </div>
                    <div>
                      <p className="font-medium text-foreground">{item.title}</p>
                      <p className="mt-1 text-sm leading-relaxed text-muted-foreground">
                        {item.body}
                      </p>
                    </div>
                  </li>
                ))}
              </ul>
            </div>

            {/* A quiet mock of the balance card. */}
            <div className="card-surface overflow-hidden">
              <div className="border-b border-border px-6 py-5">
                <p className="text-xs font-medium uppercase tracking-wider text-muted-foreground">
                  Available balance
                </p>
                <p className="tnum mt-2 text-4xl font-semibold tracking-tight text-foreground">
                  10,240.52
                  <span className="ml-2 text-lg font-medium text-muted-foreground">
                    USDT
                  </span>
                </p>
                <p className="tnum mt-1 text-sm text-muted-foreground">
                  248.00 USDT held by pending authorizations
                </p>
              </div>

              <ul className="divide-y divide-border">
                {[
                  ['Meta Ads', 'Marketing', '−184.20'],
                  ['Figma', 'SaaS Subscriptions', '−45.00'],
                  ['USDT deposit', 'Confirmed', '+2,500.00'],
                ].map(([merchant, context, amount]) => (
                  <li
                    key={merchant}
                    className="flex items-center justify-between px-6 py-3.5"
                  >
                    <div className="min-w-0">
                      <p className="truncate text-sm font-medium text-foreground">
                        {merchant}
                      </p>
                      <p className="text-xs text-muted-foreground">{context}</p>
                    </div>
                    <span
                      className={`tnum text-sm font-medium ${
                        amount.startsWith('+') ? 'text-positive' : 'text-foreground'
                      }`}
                    >
                      {amount}
                    </span>
                  </li>
                ))}
              </ul>
            </div>
          </div>
        </div>
      </section>

      {/* ---------------------------------------------------------- Closing */}
      <section className="mx-auto max-w-6xl px-5 py-16 lg:py-24">
        <div className="card-surface relative overflow-hidden px-8 py-14 text-center">
          <div
            aria-hidden
            className="pointer-events-none absolute inset-0 bg-[radial-gradient(50%_100%_at_50%_0%,var(--accent),transparent_70%)]"
          />
          <div className="relative">
            <Lock className="mx-auto size-6 text-primary" aria-hidden />
            <h2 className="mt-4 text-2xl font-semibold tracking-tight text-foreground sm:text-3xl">
              Ready to issue your first card?
            </h2>
            <p className="mx-auto mt-3 max-w-lg text-muted-foreground">
              Create an account, verify your identity, and issue a card with its
              own limits in a couple of minutes.
            </p>
            <div className="mt-7 flex flex-wrap justify-center gap-3">
              <Button asChild size="lg">
                <Link href="/signup">
                  Get started
                  <ArrowRight className="size-4" aria-hidden />
                </Link>
              </Button>
              <Button asChild size="lg" variant="secondary">
                <Link href="/security">Read about security</Link>
              </Button>
            </div>
          </div>
        </div>
      </section>
    </>
  );
}
